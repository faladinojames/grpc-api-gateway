# gRPC api gateway

This application can serve as a frontend/proxy for gRPC services, routing requests and authenticating users.

## Run

Run the backend using `go run`:

```bash
$ go run main.go
```
